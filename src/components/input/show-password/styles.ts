import styled from 'styled-components';

export const WrapIcon = styled.div`
  position: absolute;
  top: 50%;
  right: 10px;
  display: flex;
  cursor: pointer;
  transform: translateY(-50%);

  svg {
    width: 24px;
    height: 24px;
  }
`;
