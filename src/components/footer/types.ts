export interface IFooterProps {
  text: string;
  name: string;
  rules: string;
  toMain: string;
  protect: string;
  charity: string;
  toDeals: string;
  toRules: string;
  questions: string;
  toCharity: string;
  toQuestions: string;
}
