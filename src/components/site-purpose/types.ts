export interface ISitePurpose {
  regText: string;
  isHide?: boolean;
  tradeText: string;
  thingsText: string;
  showRainbow?: boolean;
}

export interface IAnimation {
  isHide: boolean;
  showRainbow: boolean;
}
